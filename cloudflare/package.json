{
  "name": "whitesim-stats-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=esm --platform=neutral --outfile=dist/worker.js",
    "test": "npm run build && node --no-warnings test/worker.test.mjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "wrangler": "^3.90.0"
  }
}
